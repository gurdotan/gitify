#### Installation
Run 
```
bash < <(curl -s https://raw.github.com/gurdotan/gitify/master/gitify )
```
and get lots of nice command line goodies for git (test only on Ubuntu).  Note: running this will not include the utilities detailed below in sections 1 & 5.
